{{!< default}}
{{!-- The tag above means - insert everything in this file into the {body} of the default.hbs template --}}
{{!-- header --}}
<header class="container">
    <div id="search">
        <div id="direct-access">
            <p>Accès direct: </p>
            <ul>
                <li><a href="#tag">Étiquettes</a></li>
                <li><a href="#main">Contenu</a></li>
            </ul>
        </div>
        <form action="">
            <input type="text" name="" id="">
            <input type="submit" value="Chercher">
        </form>
    </div>
    <nav id="main-nav" class="clearfix">
        {{#if @blog.logo}}<a class="blog-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>{{/if}}
        {{navigation}}
    </nav>
</header>
{{!-- The main content area on the homepage --}}
<main id="main" class="container" role="main">

{{!-- First static post --}}
<div class="static-post {{#if @blog.cover}}" style="background-image: url({{@blog.cover}}){{else}}no-cover{{/if}}">
    <h1>Empreinte Digital</h1>
    <div id="accroche">
        <p>L'empreinte laissée dans le monde digital n'est pas toujours celle que nous croyons.</p>
        <p>Cet univers regorge de codes, repères ou pièges pas toujours faciles à interpréter ou éviter.</p>
        <p>L'important est donc de comprendre, d'apprendre et de partager ce savoir à votre tour.</p>
    </div>
</div>
    {{!-- The tag below includes the post loop - partials/loop.hbs --}}
    {{> "loop"}}
    {{> "main-footer"}}
</main>
